services:
  mcd-mcp-proxy:
    build: .
    container_name: mcd-mcp-proxy
    ports:
      - "8080:80"
    environment:
      # Default allows all origins, change to your specific domain if needed
      - ALLOWED_ORIGIN=*
    restart: unless-stopped
